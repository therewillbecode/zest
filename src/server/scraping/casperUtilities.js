function filterLinks(regex){
    return 1

    //filteredlinks.match(regex)
}

var listingLinkRegex = /abc/;  //regular expression to match links that correspond to a listing
function add (b){return b+1}


module.exports.add = {
    filterlinks: filterLinks,
    add: add
}