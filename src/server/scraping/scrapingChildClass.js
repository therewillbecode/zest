/**
 * Created by Tom on 09/03/2016.
 */
var EventEmitter = require('events').EventEmitter;

// class that defines events for spawned casper child process to enhance communication to parent
// enhances logging
class scrapeChildProc extends EventEmitter{
// possible events that can be emitted by child process object


}

exports.classes = {scrapeChildProc};
//var MyClass = function () { … }
// MyClass.prototype.whatever = function() { … }

